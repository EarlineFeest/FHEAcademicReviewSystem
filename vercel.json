{
  "version": 2,
  "buildCommand": "cd AcademicReviewSystem && npm ci && npm run build",
  "outputDirectory": "AcademicReviewSystem/build",
  "installCommand": "cd AcademicReviewSystem && npm ci",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
